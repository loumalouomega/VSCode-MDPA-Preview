% Toolbar icon: fieldQuiver — three horizontal vector arrows of varying length
% (a quiver / vector-glyph field).
% NOTE: the committed svg-ui/fieldQuiver.svg was hand-authored to the same
% drawing (no LaTeX toolchain available); regenerating via `make ui` replaces
% it with the pdftocairo rendering of this source — same glyph either way.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, >=Stealth, x=1mm,y=1mm]
  \draw[thick,->] (-9,6) -- (3,6);
  \draw[thick,->] (-9,0) -- (9,0);
  \draw[thick,->] (-9,-6) -- (0,-6);
\end{tikzpicture}
\end{document}
