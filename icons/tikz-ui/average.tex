% Toolbar icon: average — a node (circle) and a cell (square) linked by
% double arrows, for nodal <-> elemental field averaging.
% NOTE: the committed svg-ui/average.svg was hand-authored to the same
% drawing (no pdftocairo available); regenerating via `make ui` replaces it
% with the pdftocairo rendering of this source — same glyph either way.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, x=1mm,y=1mm]
  \fill (-8,0) circle (1.6);
  \draw[thick] (2,-6) rectangle (10,2);
  \draw[thick,{Latex[length=2mm]}-{Latex[length=2mm]}] (-5.5,-1.5) -- (2,-4);
\end{tikzpicture}
\end{document}
