% Toolbar icon: problemtype — a settings/form panel (field lines) with a gear
% at its lower-right corner (configuring a simulation case).
% NOTE: the committed svg-ui/problemtype.svg was hand-authored to the same
% drawing (no LaTeX toolchain available); regenerating via `make ui` replaces
% it with the pdftocairo rendering of this source — same glyph either way.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, x=1mm,y=1mm]
  % form panel
  \draw[thick] (-11,-10) rectangle (3,10);
  \draw[thick] (-8,5) -- (0,5);
  \draw[thick] (-8,1) -- (0,1);
  \draw[thick] (-8,-3) -- (-3,-3);
  % gear: ring + teeth + hub
  \draw[thick] (7,-5) circle (3.4);
  \fill (7,-5) circle (1.3);
  \foreach \a in {0,45,...,315} {
    \draw[thick] (7,-5) ++(\a:3.4) -- ++(\a:1.6);
  }
\end{tikzpicture}
\end{document}
