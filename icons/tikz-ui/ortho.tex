% Toolbar icon: ortho — an isometric cube with strictly parallel receding
% edges (the Parallel Projection toggle: no perspective vanishing point).
% NOTE: the committed svg-ui/ortho.svg was hand-authored to the same
% drawing (no LaTeX toolchain available); regenerating via `make ui` replaces
% it with the pdftocairo rendering of this source — same glyph either way.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, x=1mm,y=1mm]
  \draw[thick] (-9,-9) rectangle (1,1);
  \draw[thick] (-9,1) -- (-4,6) -- (6,6) -- (1,1);
  \draw[thick] (1,-9) -- (6,-4) -- (6,6);
\end{tikzpicture}
\end{document}
