% Toolbar icon: stop — cancel the running operation (shown on the play button
% while an MMG run is in flight).
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, x=1mm,y=1mm]
  \fill[rounded corners=1.2mm] (-6,-6) rectangle (6,6);
\end{tikzpicture}
\end{document}
