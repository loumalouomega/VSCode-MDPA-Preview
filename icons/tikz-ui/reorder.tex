% Toolbar icon: reorder — three bars of decreasing length plus a sort arrow,
% for renumbering nodes to reduce matrix bandwidth.
% NOTE: the committed svg-ui/reorder.svg was hand-authored to the same drawing
% (no pdftocairo available); regenerating via `make ui` replaces it with the
% pdftocairo rendering of this source — same glyph either way.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, x=1mm,y=1mm]
  \draw[thick] (-9,6) -- (2,6);
  \draw[thick] (-9,0) -- (5,0);
  \draw[thick] (-9,-6) -- (-1,-6);
  \draw[thick,-{Latex[length=2.2mm]}] (8,7) -- (8,-7);
\end{tikzpicture}
\end{document}
