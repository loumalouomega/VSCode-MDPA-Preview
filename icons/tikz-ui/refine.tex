% Toolbar icon: refine — a square subdividing into 4 smaller squares, for
% uniform mesh subdivision.
% NOTE: the committed svg-ui/refine.svg was hand-authored to the same drawing
% (no pdftocairo available); regenerating via `make ui` replaces it with the
% pdftocairo rendering of this source — same glyph either way.
\documentclass[tikz,border=2mm]{standalone}
\usetikzlibrary{arrows.meta,calc,shapes.geometric}
\begin{document}
\begin{tikzpicture}[line width=1.3pt, line cap=round, line join=round, x=1mm,y=1mm]
  \draw[thick] (-9,-9) rectangle (9,9);
  \draw[thick] (0,-9) -- (0,9);
  \draw[thick] (-9,0) -- (9,0);
  \fill (0,0) circle (1.1);
  \fill (0,9) circle (0.9);
  \fill (0,-9) circle (0.9);
  \fill (9,0) circle (0.9);
  \fill (-9,0) circle (0.9);
\end{tikzpicture}
\end{document}
